/// Comentario de retroalimentación que un profesor deja sobre la actividad
/// de un alumno en una simulación concreta.
class TeacherComment {
  final String texto;
  final DateTime fecha;

  TeacherComment({required this.texto, required this.fecha});
}
